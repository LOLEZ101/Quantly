# DEPRECATED circular offline fixtures (Phase 3)

These payloads were generated by `scripts/generate-sec-fixtures.ts` from
Phase-2 curated `data/pilot/business-segments.json` and `operating-models.json`.

They remain for historical `pilot-v2-sourced` / Phase-3 regression only.

For Phase 3.5 verified publication use:

- `data/verified/sec/` (`npm run fixtures:verified`)
- `npm run phase3.5:verified`
